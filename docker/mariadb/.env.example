# .env — Variables de entorno para MariaDB (VM-04)
# Copiar a .env y rellenar con contraseñas reales.
# NUNCA subir el .env real a Git.

MYSQL_ROOT_PASSWORD=CambiarEstaContraseña
MYSQL_RSYSLOG_PASSWORD=CambiarEstaContraseña
